// This is needed for pretty angularJS routes
// These routes are needed by the server AND the client
this.angularRoutes = (function () {
  var routes = {

  };

  // Makes this file node.js friendly
  if (typeof exports !== 'undefined') {
    module.exports = routes;
  }

  return routes;
})();
